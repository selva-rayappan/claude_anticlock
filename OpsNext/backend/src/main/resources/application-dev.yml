spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging:
  level:
    io.opsnext: DEBUG
    org.springframework.security: DEBUG
    org.flywaydb: DEBUG

app:
  cookie:
    secure: false
